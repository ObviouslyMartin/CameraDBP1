<!DOCTYPE html>
<html>
<head>
    <title>A query using a subquery with IN or NOT IN</title>
</head>
<body>
<h1>select l.Lens_Manufacturer, l.Focal_Length, l.Max_Aperture_Value from Lens l where Lens_id not in
    (select Lens_id from Lens_Filter group by Filter_Id);</h1>

<table border="1">
    <tr>
        <th>Lens Manufacturer</th>
        <th>Focal Length</th>
        <th>Max_Aperture_Value</th>
    </tr>
    <%for(var i=0; i< stuff.length; i++ ) { %>
    <tr>
        <td><%= stuff[i].Lens_Manufacturer%></td>
        <td><%= stuff[i].Focal_Length%></td>
        <td><%= stuff[i].Max_Aperture_Value%></td>
    </tr>
    <%}%>
</table>
</body>
</html>